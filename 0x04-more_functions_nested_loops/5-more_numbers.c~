#include "main.h"

/**
 * more_numbers - prints 0 - 14 , ten times
 *
 *
 *
 * Return: 0
 */

void more_numbers(void)
{
	int Ebube;

	while (Ebube = '0')
	{
		while (Ebube <= 14)
		{
			_putchar ('\n');
			Ebube++;
		}
		_putchar (Ebube);
	}
}
